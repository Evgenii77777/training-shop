export const getColor = (state) => state.basket.color;
export const getSize = (state) => state.basket.size;
export const getIsEmpty = (state) => state.basket.card;
export const getOpen = (state) => state.basket.visible;
