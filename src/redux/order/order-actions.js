import { createAction } from "@reduxjs/toolkit";

export const addPickup = createAction("add/Pickup");
export const addCart = createAction("add/Cart");
export const addProducts = createAction("add/Products");
